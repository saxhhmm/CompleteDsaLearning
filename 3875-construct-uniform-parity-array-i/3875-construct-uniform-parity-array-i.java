class Solution {
    public boolean uniformArray(int[] nums1) {
        // Since we can always force the array to be uniformly odd (Even - Odd = Odd) 
        // or keep it uniform if it already is, it is always possible.
        return true;
    }
}